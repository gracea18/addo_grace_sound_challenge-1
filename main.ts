while (true) {
    input.soundLevel()
    if (input.soundLevel() > 5) {
        light.setPixelColor(0, light.rgb(255, 128, 0))
        pause(100)
        light.setPixelColor(1, light.rgb(239, 255, 11))
        pause(100)
        light.setPixelColor(2, light.rgb(26, 255, 0))
        pause(100)
        light.setPixelColor(3, light.rgb(0, 255, 175))
        pause(100)
        light.setPixelColor
    }
    
}
